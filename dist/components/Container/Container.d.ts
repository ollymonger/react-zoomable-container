import React from "react";
declare const Container: ({ scale, position, children }: {
    scale: number;
    position: {
        x: number;
        y: number;
    };
    children: React.ReactNode;
}) => JSX.Element;
export { Container };
